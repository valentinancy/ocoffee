export interface IMenu {
    name: string;
    price: number;
    description: string;
    image: string;
}

export interface IPurchaseDetail {
    name: string;
    phone: number;
    merchant: string;
    notes: string;
}
